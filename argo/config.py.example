DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'postgres',
        'USER': 'postgres',
        'HOST': 'argo-db',
        'PORT': 5432,
    }
}

ALLOWED_HOSTS = ['localhost', 'argo-web']

BASE_DIR = ''

STATIC_ROOT = '/static/'

ELASTICSEARCH_DSL = {
    'default': {
        'hosts': ['elasticsearch:9200']
    },
}
